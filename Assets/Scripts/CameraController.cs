using System.Collections; using System.Collections.Generic; using UnityEngine;  public class CameraController : MonoBehaviour {      public GameObject player;      private Vector3 offset;  	// Use this for initialization 	void Start () {         offset = transform.position - player.transform.position; 	} 	 	// Update is called once per frame 	void LateUpdate () {         transform.position = player.transform.position + offset; 	} }  /*    *  cree une case prefab  *  script execute dans editor  *  crée une grille de 10x10  *  classe plateau avec tableau 2D dans lequel j'ai la logic des cases instantiées  *  apres ca j'ai la grille : si je connais un emplacement sur une grille je connais ses voisins  *  ray cast : vecotr qui part dans une direction et fais voir ce que je croise -> part sous mon pingiouin pour savoir la case du dessous  *  au moment on crée la grille :! chaque case a  un objet mais avec script ou dedans ca donne ses coordonées  *  pingouin fait un xrai en dessous et voit qu'il y a une case -> recupere l'objet case et on sait ou on est sur la grille  *  a partir de la grille   *  pingouin avec une detection decollision  *    */